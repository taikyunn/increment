// tsには大文字で始まるBoolean型もあるがbooleanはまた別の型である
const isOk: boolean = true;

// tsにて数値型の型注釈にはnumberを使用する。Numberとは別物である。
const number: number = 123;

// 文字列型の型注釈
const message: string = "Hello";

// nullの型注釈
const i: null = null;

// undefinedの型注釈
const u: undefined = undefined;
